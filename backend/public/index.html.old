<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TeamNL Cloud9 - API Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  
  <!-- Header -->
  <header class="bg-gradient-to-r from-blue-600 to-indigo-700 text-white shadow-lg">
    <div class="container mx-auto px-6 py-8">
      <h1 class="text-4xl font-bold">🏁 TeamNL Cloud9 Racing</h1>
      <p class="text-blue-100 mt-2">Backend API v2.0 - Clean Production</p>
    </div>
  </header>

  <div class="container mx-auto px-6 py-8 max-w-5xl">
    
    <!-- Status Card -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-8">
      <div class="flex items-center justify-between mb-4">
        <h2 class="text-2xl font-bold text-gray-800">📊 System Status</h2>
        <div id="status-badge" class="px-4 py-2 rounded-full text-sm font-semibold bg-gray-200 text-gray-600">
          Checking...
        </div>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-6">
        <div class="text-center p-4 bg-blue-50 rounded-lg">
          <div class="text-3xl font-bold text-blue-600" id="backend-status">-</div>
          <div class="text-sm text-gray-600 mt-1">Backend</div>
        </div>
        <div class="text-center p-4 bg-green-50 rounded-lg">
          <div class="text-3xl font-bold text-green-600" id="database-status">-</div>
          <div class="text-sm text-gray-600 mt-1">Database</div>
        </div>
        <div class="text-center p-4 bg-purple-50 rounded-lg">
          <div class="text-3xl font-bold text-purple-600" id="api-status">-</div>
          <div class="text-sm text-gray-600 mt-1">ZwiftRacing API</div>
        </div>
      </div>

      <div class="mt-6 p-4 bg-gray-50 rounded text-sm font-mono">
        <div><strong>Backend URL:</strong> <span id="backend-url" class="text-blue-600">-</span></div>
        <div class="mt-2"><strong>Version:</strong> <span id="version">-</span></div>
        <div class="mt-2"><strong>Last Check:</strong> <span id="last-check">-</span></div>
      </div>
    </div>

    <!-- API Endpoints -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-8">
      <h2 class="text-2xl font-bold text-gray-800 mb-6">🔗 API Endpoints</h2>
      
      <div class="space-y-4">
        <!-- Endpoint 1: Clubs -->
        <div class="border-l-4 border-blue-500 pl-4 py-3 bg-blue-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-blue-600 text-white text-xs font-bold rounded">GET</span>
              <span class="ml-3 font-mono text-sm">/api/clubs/:clubId</span>
            </div>
            <button onclick="testEndpoint('/api/clubs/11818')" 
                    class="px-3 py-1 bg-blue-600 text-white text-xs rounded hover:bg-blue-700">
              Test
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Haal club informatie op</p>
        </div>

        <!-- Endpoint 2: Riders -->
        <div class="border-l-4 border-green-500 pl-4 py-3 bg-green-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-green-600 text-white text-xs font-bold rounded">GET</span>
              <span class="ml-3 font-mono text-sm">/api/riders</span>
            </div>
            <button onclick="testEndpoint('/api/riders?limit=10')" 
                    class="px-3 py-1 bg-green-600 text-white text-xs rounded hover:bg-green-700">
              Test
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Haal riders lijst op (optioneel: ?clubId=11818)</p>
        </div>

        <!-- Endpoint 3: Events -->
        <div class="border-l-4 border-purple-500 pl-4 py-3 bg-purple-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-purple-600 text-white text-xs font-bold rounded">GET</span>
              <span class="ml-3 font-mono text-sm">/api/events</span>
            </div>
            <button onclick="testEndpoint('/api/events?limit=10')" 
                    class="px-3 py-1 bg-purple-600 text-white text-xs rounded hover:bg-purple-700">
              Test
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Haal events lijst op (optioneel: ?clubId=11818)</p>
        </div>

        <!-- Endpoint 4: Results -->
        <div class="border-l-4 border-yellow-500 pl-4 py-3 bg-yellow-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-yellow-600 text-white text-xs font-bold rounded">GET</span>
              <span class="ml-3 font-mono text-sm">/api/results/:eventId</span>
            </div>
            <button onclick="alert('Voer eerst event sync uit om data te hebben')" 
                    class="px-3 py-1 bg-yellow-600 text-white text-xs rounded hover:bg-yellow-700">
              Test
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Haal race resultaten op voor specifiek event</p>
        </div>

        <!-- Endpoint 5: Rider History -->
        <div class="border-l-4 border-red-500 pl-4 py-3 bg-red-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-red-600 text-white text-xs font-bold rounded">GET</span>
              <span class="ml-3 font-mono text-sm">/api/history/:riderId</span>
            </div>
            <button onclick="alert('Voer eerst rider history sync uit om data te hebben')" 
                    class="px-3 py-1 bg-red-600 text-white text-xs rounded hover:bg-red-700">
              Test
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Haal historische rider data op</p>
        </div>

        <!-- Endpoint 6: Sync Logs -->
        <div class="border-l-4 border-indigo-500 pl-4 py-3 bg-indigo-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-indigo-600 text-white text-xs font-bold rounded">GET</span>
              <span class="ml-3 font-mono text-sm">/api/sync-logs</span>
            </div>
            <button onclick="testEndpoint('/api/sync-logs')" 
                    class="px-3 py-1 bg-indigo-600 text-white text-xs rounded hover:bg-indigo-700">
              Test
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Haal sync operatie logs op</p>
        </div>
      </div>
    </div>

    <!-- Sync Endpoints -->
    <div class="bg-white rounded-lg shadow-md p-6">
      <h2 class="text-2xl font-bold text-gray-800 mb-6">🔄 Sync Endpoints (POST)</h2>
      
      <div class="space-y-4">
        <div class="border-l-4 border-orange-500 pl-4 py-3 bg-orange-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-orange-600 text-white text-xs font-bold rounded">POST</span>
              <span class="ml-3 font-mono text-sm">/api/clubs/:clubId/sync</span>
            </div>
            <button onclick="syncData('/api/clubs/11818/sync', 'club')" 
                    class="px-3 py-1 bg-orange-600 text-white text-xs rounded hover:bg-orange-700">
              Sync Club
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Synchroniseer club data vanuit ZwiftRacing API</p>
        </div>

        <div class="border-l-4 border-orange-500 pl-4 py-3 bg-orange-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-orange-600 text-white text-xs font-bold rounded">POST</span>
              <span class="ml-3 font-mono text-sm">/api/riders/sync</span>
            </div>
            <button onclick="syncData('/api/riders/sync', 'riders')" 
                    class="px-3 py-1 bg-orange-600 text-white text-xs rounded hover:bg-orange-700">
              Sync Riders
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Synchroniseer club riders vanuit ZwiftRacing API</p>
        </div>

        <div class="border-l-4 border-orange-500 pl-4 py-3 bg-orange-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-orange-600 text-white text-xs font-bold rounded">POST</span>
              <span class="ml-3 font-mono text-sm">/api/events/sync</span>
            </div>
            <button onclick="syncData('/api/events/sync', 'events')" 
                    class="px-3 py-1 bg-orange-600 text-white text-xs rounded hover:bg-orange-700">
              Sync Events
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Synchroniseer club events vanuit ZwiftRacing API</p>
        </div>

        <div class="border-l-4 border-orange-500 pl-4 py-3 bg-orange-50">
          <div class="flex items-center justify-between">
            <div>
              <span class="px-2 py-1 bg-orange-600 text-white text-xs font-bold rounded">POST</span>
              <span class="ml-3 font-mono text-sm">/api/sync-logs/full-sync</span>
            </div>
            <button onclick="syncData('/api/sync-logs/full-sync', 'full')" 
                    class="px-3 py-1 bg-orange-600 text-white text-xs rounded hover:bg-orange-700">
              Full Sync
            </button>
          </div>
          <p class="text-sm text-gray-600 mt-2">Volledige sync: club + riders + events</p>
        </div>
      </div>
    </div>

    <!-- Test Output -->
    <div id="test-output" class="mt-8 bg-gray-900 text-green-400 rounded-lg p-4 font-mono text-sm hidden">
      <div class="flex items-center justify-between mb-2">
        <span class="font-bold">API Response:</span>
        <button onclick="document.getElementById('test-output').classList.add('hidden')" 
                class="text-gray-400 hover:text-white">✕</button>
      </div>
      <pre id="test-result" class="overflow-x-auto whitespace-pre-wrap"></pre>
    </div>

  </div>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white py-6 mt-12">
    <div class="container mx-auto px-6 text-center">
      <p class="text-sm">TeamNL Cloud9 Racing Team - Backend API v2.0</p>
      <p class="text-xs text-gray-400 mt-2">Production Ready • Supabase • ZwiftRacing API</p>
    </div>
  </footer>

  <script>
    // Configuration
    const API_BASE_URL = window.location.origin;

    // Check system status on load
    async function checkStatus() {
      try {
        const response = await fetch(`${API_BASE_URL}/health`);
        const data = await response.json();
        
        if (data.status === 'ok') {
          document.getElementById('status-badge').className = 'px-4 py-2 rounded-full text-sm font-semibold bg-green-100 text-green-800';
          document.getElementById('status-badge').textContent = '✓ Online';
          
          document.getElementById('backend-status').textContent = '✓ OK';
          document.getElementById('database-status').textContent = '✓ OK';
          document.getElementById('api-status').textContent = '⚠ Ready';
          
          document.getElementById('backend-url').textContent = API_BASE_URL;
          document.getElementById('version').textContent = data.version || 'v2.0.0';
          document.getElementById('last-check').textContent = new Date(data.timestamp).toLocaleString('nl-NL');
        }
      } catch (error) {
        document.getElementById('status-badge').className = 'px-4 py-2 rounded-full text-sm font-semibold bg-red-100 text-red-800';
        document.getElementById('status-badge').textContent = '✕ Offline';
        
        document.getElementById('backend-status').textContent = '✕ Error';
        document.getElementById('database-status').textContent = '?';
        document.getElementById('api-status').textContent = '?';
      }
    }

    // Test endpoint
    async function testEndpoint(path) {
      const output = document.getElementById('test-output');
      const result = document.getElementById('test-result');
      
      output.classList.remove('hidden');
      result.textContent = `Fetching ${API_BASE_URL}${path}...\n`;
      
      try {
        const response = await fetch(`${API_BASE_URL}${path}`);
        const data = await response.json();
        
        result.textContent = JSON.stringify(data, null, 2);
      } catch (error) {
        result.textContent = `Error: ${error.message}`;
      }
    }

    // Sync data
    async function syncData(path, type) {
      if (!confirm(`Start ${type} synchronisatie?\n\nDit haalt data op van ZwiftRacing API en slaat het op in Supabase.`)) {
        return;
      }

      const output = document.getElementById('test-output');
      const result = document.getElementById('test-result');
      
      output.classList.remove('hidden');
      result.textContent = `Syncing ${type}...\nDit kan 5-30 seconden duren...\n`;
      
      try {
        const response = await fetch(`${API_BASE_URL}${path}`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' }
        });
        const data = await response.json();
        
        result.textContent = JSON.stringify(data, null, 2);
        
        if (data.success || response.ok) {
          alert(`✅ ${type} sync succesvol!\n\nRecords: ${data.count || data.syncedRiders?.length || data.syncedEvents?.length || '?'}`);
        }
      } catch (error) {
        result.textContent = `Error: ${error.message}`;
        alert(`❌ ${type} sync gefaald: ${error.message}`);
      }
    }

    // Auto-refresh status every 30s
    checkStatus();
    setInterval(checkStatus, 30000);
  </script>

</body>
</html>
