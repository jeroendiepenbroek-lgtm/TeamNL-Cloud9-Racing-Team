# Environment variabelen voor TeamNL Cloud9 Backend

# Supabase
SUPABASE_URL=https://bktbeefdmrpxhsyyalvc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ZwiftRacing API
ZWIFT_API_KEY=650c6d2fc4ef6858d74cbef1

# Server
PORT=3000
NODE_ENV=development

# US8: Auto-Sync Configuration (Legacy - use V2 below)
SYNC_ENABLED=true
SYNC_INTERVAL_HOURS=6
SYNC_START_DELAY_MINUTES=5

# V2 Sync Configuration (Recommended)
# ============================================================================
# Rider Sync - Syncs team member data from ZwiftRacing API
RIDER_SYNC_ENABLED=true
RIDER_SYNC_INTERVAL_MINUTES=60          # Min: 60 (API rate limit: 1/15min)

# Event Sync - Syncs upcoming events and signups
EVENT_SYNC_ENABLED=true
NEAR_EVENT_SYNC_INTERVAL_MINUTES=15     # Frequent updates for near events (4x/hour)
FULL_EVENT_SYNC_INTERVAL_HOURS=3        # Full scan every 3 hours
NEAR_EVENT_THRESHOLD_MINUTES=120        # Events within 2 hours = "near"
LOOKFORWARD_HOURS=48                     # Fetch events in next 48 hours

# ⚠️  IMPORTANT: Validator will prevent invalid configurations
# - NEAR and FULL syncs must not overlap (auto-adjusted to :55)
# - Rate limits are enforced (min 15 min for riders, 5 min for events)
# - See docs/SYNC_SERVICES_ISSUES.md for details
