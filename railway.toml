# Railway Configuration - NUCLEAR OPTION (Dec 7, 2025)
# Use simplest possible Dockerfile - NO optimization

[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile.simple"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
