# Railway Configuration - DEFINITIVE (Dec 7, 2025)
# ROOT context + backend Dockerfile

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "cd backend && npx tsx src/server.ts"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
