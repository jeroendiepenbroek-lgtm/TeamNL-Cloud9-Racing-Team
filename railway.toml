# Railway Configuration - DEFINITIVE (Dec 7, 2025)
# WORKAROUND: Railway ignores root Dockerfile when rootDir=backend is set

[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "npx tsx src/server.ts"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
